<%= form_with(model: @package, local: true) do |form| %>
  <% if @package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@package.errors.count, "error") %> prohibited this package from being saved:</h2>

      <ul>
        <% @package.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "Tracking Number:" %><br>
    <%= form.text_field :tracking %>
  </div>

  <div class="field">
    <%= form.label "Carrier:" %>
    <%= form.select :carrier, [['UPS','UPS']],{},{class: "form-control col-md-1"}%>
  </div>

  <div class="actions">
    <%= form.submit 'Add Package', :class => 'btn btn-primary my-2' %>
  </div>
<% end %>
